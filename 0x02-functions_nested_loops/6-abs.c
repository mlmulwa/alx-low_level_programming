#include "main.h"

/**
 * _abs - print absolute value
 * @n: number being tested
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
